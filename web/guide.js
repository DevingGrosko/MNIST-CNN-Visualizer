export const GUIDE=[
['What is a feature map actually showing?',`A feature map records one filter’s response at every spatial position. It is a grid of numbers, not a picture the network consciously recognizes. Bright areas mean larger positive responses on this display; blue shows negative responses before ReLU. The map can resemble edges or strokes because the filter responds to local patterns in the digit.<br><br>A learned filter is optimized to help the final classification task. Calling a particular filter an “edge detector” can be a useful description, but its role is not assigned in advance. Some filters will be difficult to describe in words. This lab rescales each heatmap independently for visibility; use the inspector when comparing numeric magnitudes.`],
['Why use convolution instead of connecting every pixel?',`Convolution combines local connectivity with shared weights. A 3 × 3 filter sees a small neighborhood, and the same weights are used at every image position. In the first layer, eight filters and eight biases require only 80 parameters. A separate fully connected mapping from 784 pixels to 6,272 outputs would require millions.<br><br>Weight sharing lets a filter recognize a local pattern in multiple locations. That gives convolution translation equivariance away from boundary effects: shifting the input shifts its response. Pooling can reduce sensitivity to small movements, but it does not make the full network perfectly translation invariant.`],
['How far can one neuron “see” into the image?',`A receptive field is the region of the input that can affect an activation. In this architecture, a first-convolution activation spans 3 × 3 input pixels. After the first pool it spans 4 × 4, and its neighbors are two input pixels apart. The second convolution expands that span to 8 × 8; the second pool expands it to 10 × 10, with a spacing of four input pixels.<br><br>These are theoretical receptive fields. At image borders part of the region falls in zero padding. ReLU and max pooling can make some paths inactive for a particular image. Dense neurons connect to all pooled features and can combine evidence from across the whole digit.`],
['How are kernels, channels, and neurons related?',`A channel is one slice of a tensor. A filter produces one output channel, but it spans every input channel. Conv 1 has eight filters shaped [1, 3, 3]. Conv 2 has sixteen filters shaped [8, 3, 3]. One Conv 2 output value sums 72 products and one bias: nine products per channel across eight channels.<br><br>Each spatial output is an activation of the convolutional layer. Outputs in the same channel reuse the filter’s weights. In a dense layer each output neuron instead has its own weight for every incoming activation. The lab’s dense inspector lists every connection for your selected neuron.`],
['Training, inference, and backpropagation: what changes?',`Inference is the forward calculation: an image flows through fixed weights and produces logits and probabilities. The live network view performs inference whenever the image or weights change. It records all intermediate tensors so you can inspect them.<br><br>Training adds a target label, a loss, a backward pass, and an optimizer update. Backpropagation calculates derivatives; it does not itself change weights. The optimizer uses those derivatives to update the parameters. The Learning step separates those ideas and uses an explicit SGD update. The exported checkpoint was trained with Adam using only the MNIST training split.`],
['Why can a confident prediction be wrong?',`Softmax distributes probability among the model’s ten available classes. It has no “none of these” output. A letter, blank image, or unfamiliar handwriting can still receive a high score for a digit. Confidence is also affected by training and calibration; it is not a guarantee of correctness.<br><br>Test accuracy measures the fraction of correct predictions over a held-out set. It does not promise the same accuracy on your own drawings, photos, or every digit category. The bundled examples are the first five occurrences of each label in the MNIST test split, not samples chosen for successful predictions.`],
['The equations and original implementation',`The architecture and model probes come from <a href="https://github.com/DevingGrosko/MNIST-CNN-Visualizer" target="_blank" rel="noreferrer">Devin Grosko’s MNIST CNN Visualizer</a>. The website’s JavaScript forward pass is checked against independently exported PyTorch tensors for both the initial and trained checkpoints.<br><br>For further study: <a href="https://cs231n.github.io/convolutional-networks/" target="_blank" rel="noreferrer">Stanford CS231n’s convolutional network notes</a> explain the layer mechanics; <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.Conv2d.html" target="_blank" rel="noreferrer">PyTorch’s Conv2d documentation</a> specifies cross-correlation and tensor shapes; <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html" target="_blank" rel="noreferrer">CrossEntropyLoss</a> documents the loss used in the original training pipeline.`]
];
